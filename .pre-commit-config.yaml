# Keep in sync with .github/workflows/validate.yml
repos:
  - repo: local
    hooks:
      - id: ci-parity
        name: Spec validation (spectral + asyncapi + pytest + examples)
        entry: scripts/hooks/ci-check.sh
        language: script
        pass_filenames: false
        always_run: true
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
        args: [--maxkb=500]
      - id: detect-private-key
